[build]
builder = "NIXPACKS"
buildCommand = "mvn -B -DskipTests clean package"

[deploy]
startCommand = "java -jar target/management-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
